# Copy this file to ~/.config/race-aggregator-v2/github-actions.env
# and replace the placeholder with a GitHub token that can trigger Actions
# on jonkan999/race-aggregator-v2.

export GITHUB_ACTIONS_TOKEN=replace_me
